<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--namespace:接口的全限定名称-->
<mapper namespace="mapper.CategoryDao">
    <!--id:方法的名称  resultType:返回值类型(若为对象，为全限定名称或别名))-->
    <select id="selectAllCategory" resultType="entity.Category">
        select *
        from category;
    </select>

    <insert id="insertCategory">
        insert into category(CATE_ID, CATE_NAME, CATE_PARENT_ID)
        values (null, #{CATE_NAME}, #{CATE_PARENT_ID})
    </insert>

    <delete id="deleteCategory">
        delete
        from category
        where CATE_ID = #{CATE_ID}
    </delete>

    <update id="updateCategory">
        update category
        set CATE_NAME=#{CATE_NAME},
            CATE_PARENT_ID=#{CATE_PARENT_ID}
        where CATE_ID = #{CATE_ID}
    </update>

    <select id="selectFatherCategory" resultType="entity.Category">
        select CATE_ID, CATE_NAME, CATE_PARENT_ID from category where CATE_PARENT_ID = 0;
    </select>

    <select id="selectSonCategory" resultType="entity.Category">
        select CATE_ID, CATE_NAME, CATE_PARENT_ID from category where CATE_PARENT_ID != 0;
    </select>
</mapper>